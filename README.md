# hospital_manager
This is a simple Python project made for managing basic hospital patient records. The program works in the terminal and allows you to add, view, search, update and delete patient details. All data is stored locally inside a JSON file, so no database setup is needed.
